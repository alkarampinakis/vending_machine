Options -Indexes

RewriteEngine On

# Route /api/* to index.php (Laravel)
RewriteCond %{REQUEST_URI} ^/api
RewriteRule ^ index.php [L]

# Serve existing files directly
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# SPA fallback
RewriteRule ^ index.php [L]
